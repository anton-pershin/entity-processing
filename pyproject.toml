[project]
name = "entity-processing"
version = "0.1.0"
description = "Prompt-based entity, sentiment, and relation extraction"
requires-python = ">= 3.12"
dependencies = [
    "hydra-core>=1.3",
    "rally",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
